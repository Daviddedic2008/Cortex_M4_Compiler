-- TODO --

* [being worked on]


* improve register snapshot system to ensure dirty bit tracking properly
* start adding actual hex emission, the current IR is VERY close to a true binary, but requires some bit shifting stuff.
	* associated issue: make sure the backpatches actually patch the PROPER bits, not just the proper memory location.



~ [done]


~ add register file snapshot storing/restoring before and after conditionals(if/else, while)
~ make it so equal opcode always writes to new register instead of just returning old regs
  e.g. x = y, have it copy value of y from one reg to another. dont just lazily return the regs y is already in.
  * add logical and, or, not
~ add bitwise not
~ make sure backpatching uses proper bit packing
~ add increment and decrement ops, of type += and -=. These should write back to old registers instead of always finding new ones
~ add else, continue, break